Some original leftover Perl scripts of MBSP.
They are now deprecated by pure Python implementations (see mbsp.py).
We keep them around for benchmark testing (Python implementations must perfom equally well or better).